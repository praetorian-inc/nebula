id: vm_secrets_debug
name: VM Debug Test
category: ["debug"]
description: Debug template to test VM resource discovery
severity: High
reportability: Automatic
triageNotes: Debug template for testing
references:
  - https://learn.microsoft.com/en-us/azure/virtual-machines/
query: |
  resources
  | where type =~ 'Microsoft.Compute/virtualMachines'
  | where name =~ 'test-find-secrets-vm-rjwl'
  | project
      id,
      type,
      name,
      location,
      subscriptionId,
      resourceGroup